import { Icon } from "@iconify/react"
import { useState } from "react"

export default function TablePaginations({ children }) {

   

    return (
        <div className="table-pagination">
           {children}
        </div>
    )
}
