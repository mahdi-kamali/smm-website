import React from 'react'

const TableHeader = (props) => {


    return (
        <div className='table-header'>
            {props.children}
        
        </div>
    )
}

export default TableHeader